# AGENTS instructions

- このリポジトリで作業するエージェントは、依存関係のインストール・スクリプト実行・チェック実行に **必ず Bun を使ってください**。
- `npm` / `pnpm` / `yarn` は使用しないでください。
- 代表的な実行例:
  - `bun install`
  - `bun run build`
  - `bun run check`
  - `bun run format`
